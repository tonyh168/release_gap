# Ascend/GLM-4-32B-0414 适配与评测记录

- **日期**：`2026-09-20`
- **节点**：`10.55.0.173` / `bm-jn-zs-zone1-910C-64G-10-117`
- **模型**：`zai-org/GLM-4-32B-0414`
- **结论**：Ascend 910C TP2 部署成功；GPQA Diamond 50 题 `54.00%`，NV `55.00%`，相对退化 `1.82%`，通过。

## 环境与部署

| 项目 | 值 |
|---|---|
| 镜像 | `harbor.baai.ac.cn/flagrelease-public/flagrelease_ascend_vllm020plugin_base:no_vllm_ascend` |
| 容器 | `GLM-4-32B-0414_flagos` |
| vLLM / plugin-FL | `0.20.2` / `0.2.0+g1326a3374` |
| 模型路径 | `/public-flash/models/GLM-4-32B-0414` -> `/models/GLM-4-32B-0414` |
| 逻辑设备 | `12,13` |
| TP / dtype | `2` / `bfloat16` |
| 端口 | `8005` |
| max model len | `32768` |

权重校验：14 个 safetensors 分片，总字节数 `65,132,235,080`，无缺失分片。

```bash
export VLLM_WORKER_MULTIPROC_METHOD=spawn
vllm serve /models/GLM-4-32B-0414 \
  --host 0.0.0.0 --port 8005 \
  --served-model-name GLM-4-32B-0414 \
  --dtype bfloat16 --tensor-parallel-size 2 \
  --max-model-len 32768 --gpu-memory-utilization 0.90 \
  --enforce-eager --trust-remote-code
```

`/health` 返回 HTTP 200，实际 chat 请求 `2+2` 返回 `4`。

## GPQA 50 题

EvalScope `1.5.1`，`eval_batch_size=4`，`temperature=0`，`max_tokens=4096`，耗时 `1072.01s`。

| 项目 | 值 |
|---|---:|
| Ascend | `54.00%` / `27/50` |
| NV | `55.00%` |
| 绝对差 | `-1.00` 个百分点 |
| 相对退化 | `1.82%` |
| Runaway | `0` |
| Parser mismatch | `0` |
| 判定 | 通过 |

结果：

```text
/public-flash/models/release_run_logs/GLM-4-32B-0414/ascend/gpqa50/gpqa50.json
/public-flash/models/release_run_logs/GLM-4-32B-0414/ascend/gpqa50/verdict.json
/public-flash/models/release_run_logs/GLM-4-32B-0414/ascend/gpqa50/eval.log
```

### 通过题号（1 基）

```text
1, 2, 3, 5, 6, 8, 9, 12, 13, 14, 15, 17, 20, 21,
24, 27, 28, 30, 32, 34, 36, 39, 40, 42, 45, 47, 48
```

### 失败题号（1 基）

```text
4, 7, 10, 11, 16, 18, 19, 22, 23, 25, 26, 29,
31, 33, 35, 37, 38, 41, 43, 44, 46, 49, 50
```

## 可复用规则

32B BF16 模型在本机两个 64GB 逻辑设备上可用 TP2 运行 32768 上下文；下载遇到代理断流应保留 `.incomplete` 断点续传，启动前必须核对权重索引的所有分片。
